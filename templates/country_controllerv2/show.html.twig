{% extends 'base.html.twig' %}

{% block title %}Country{% endblock %}

{% block body %}
    <h1>Country</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ country.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_country_controllerv2_index') }}">back to list</a>

    <a href="{{ path('app_country_controllerv2_edit', {'id': country.id}) }}">edit</a>

    {{ include('country_controllerv2/_delete_form.html.twig') }}
{% endblock %}
